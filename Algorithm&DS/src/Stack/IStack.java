/**
 * 
 */
package Stack;

import LinkedList.Node;

/**
 * @author RC
 *
 */
interface IStack {

//	public Node Push(Node element, int data);
//	public Node Pop(Node element);
	int CountElement(Node Element);
	public boolean IsStackEmpty(Node element);
	int sizeofStack(Node element);
	
	
}
